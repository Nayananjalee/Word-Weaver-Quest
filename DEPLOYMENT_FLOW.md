# 🎯 Deployment Flow Diagram

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         YOUR APPLICATION                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐              ┌──────────────────┐        │
│  │   FRONTEND       │◄────────────►│    BACKEND       │        │
│  │   (React App)    │   API Calls  │  (FastAPI)       │        │
│  │                  │              │                  │        │
│  │  - User Interface│              │  - Story Gen     │        │
│  │  - Camera Tracking│             │  - ML Models     │        │
│  │  - Visualizations│              │  - API Endpoints │        │
│  └──────────────────┘              └──────────────────┘        │
│         │                                   │                   │
│         │                                   │                   │
│         ▼                                   ▼                   │
│  ┌──────────────────┐              ┌──────────────────┐        │
│  │    Vercel        │              │     Render       │        │
│  │  (Free Hosting)  │              │  (Free Hosting)  │        │
│  └──────────────────┘              └──────────────────┘        │
│                                             │                   │
│                                             │                   │
│                                             ▼                   │
│                                    ┌──────────────────┐        │
│                                    │   Supabase DB    │        │
│                                    │   (PostgreSQL)   │        │
│                                    └──────────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

---

## Deployment Steps Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     DEPLOYMENT PROCESS                          │
└─────────────────────────────────────────────────────────────────┘

STEP 1: PREPARE
┌────────────────────────────────────────────┐
│  ✓ Code committed to GitHub                │
│  ✓ Environment variables ready             │
│  ✓ Accounts created (Vercel + Render)      │
└────────────────────────────────────────────┘
              │
              ▼
STEP 2: DEPLOY BACKEND
┌────────────────────────────────────────────┐
│  1. Go to Render Dashboard                 │
│  2. New Web Service → Connect GitHub       │
│  3. Select repository                      │
│  4. Root Directory: backend                │
│  5. Add environment variables:             │
│     - GOOGLE_API_KEY                       │
│     - SUPABASE_URL                         │
│     - SUPABASE_KEY                         │
│  6. Deploy (wait ~10 min)                  │
│  7. ✅ Copy backend URL                    │
└────────────────────────────────────────────┘
              │
              ▼
STEP 3: DEPLOY FRONTEND
┌────────────────────────────────────────────┐
│  1. Go to Vercel Dashboard                 │
│  2. Import Project → Select GitHub repo    │
│  3. Root Directory: frontend               │
│  4. Add environment variable:              │
│     - REACT_APP_API_URL = <backend URL>    │
│  5. Deploy (wait ~3 min)                   │
│  6. ✅ Get your live URL                   │
└────────────────────────────────────────────┘
              │
              ▼
STEP 4: VERIFY
┌────────────────────────────────────────────┐
│  ✓ Test backend: /docs endpoint            │
│  ✓ Test frontend: Open live URL            │
│  ✓ Verify API connection                   │
│  ✓ Test key features                       │
└────────────────────────────────────────────┘
              │
              ▼
        🎉 DONE!
```

---

## Data Flow in Production

```
User's Browser
     │
     │ (1) Visits App
     ▼
┌─────────────────┐
│  Vercel CDN     │ ◄── React App (Static Files)
│  (Frontend)     │
└─────────────────┘
     │
     │ (2) User Interaction
     │     (e.g., "Generate Story")
     │
     ▼
┌─────────────────┐
│  API Request    │ ──► fetch(`${API_BASE_URL}/generate-story`)
└─────────────────┘
     │
     │ (3) HTTPS Request
     ▼
┌─────────────────┐
│  Render Server  │ ◄── FastAPI Backend
│  (Backend)      │     Python + ML Models
└─────────────────┘
     │
     │ (4) Database Query
     ▼
┌─────────────────┐
│  Supabase       │ ◄── PostgreSQL Database
│  (Database)     │     User Data + Progress
└─────────────────┘
     │
     │ (5) AI Processing
     ▼
┌─────────────────┐
│  Google Gemini  │ ◄── Story Generation
│  (AI API)       │
└─────────────────┘
     │
     │ (6) Response Flow
     ▼
Backend → Frontend → User's Browser
```

---

## Environment Variables Flow

```
Development (Local)
┌──────────────────────────────────────┐
│  backend/.env                        │
│  ├── GOOGLE_API_KEY=xxx              │
│  ├── SUPABASE_URL=xxx                │
│  └── SUPABASE_KEY=xxx                │
└──────────────────────────────────────┘

Production (Cloud)
┌──────────────────────────────────────┐
│  Render Environment (Backend)        │
│  ├── GOOGLE_API_KEY → Render UI      │
│  ├── SUPABASE_URL → Render UI        │
│  └── SUPABASE_KEY → Render UI        │
└──────────────────────────────────────┘
┌──────────────────────────────────────┐
│  Vercel Environment (Frontend)       │
│  └── REACT_APP_API_URL → Vercel UI   │
└──────────────────────────────────────┘
```

---

## Auto-Deployment Flow

```
Developer Workflow
┌─────────────────────────────────────────────────┐
│  1. Make code changes locally                   │
│  2. git add . && git commit -m "Update"         │
│  3. git push origin main                        │
└─────────────────────────────────────────────────┘
                │
                ▼
        GitHub Repository
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
  Vercel Webhook   Render Webhook
        │               │
        ▼               ▼
  Build Frontend   Build Backend
   (~3 minutes)    (~10 minutes)
        │               │
        ▼               ▼
     Deploy          Deploy
        │               │
        └───────┬───────┘
                ▼
         ✅ Live Update!
```

---

## Cost Breakdown (FREE TIER)

```
┌─────────────────────────────────────────────────┐
│  SERVICE      │  COST    │  LIMITS             │
├─────────────────────────────────────────────────┤
│  Render       │  $0/mo   │  750 hrs, sleeps    │
│  Vercel       │  $0/mo   │  100GB bandwidth    │
│  Supabase     │  $0/mo   │  500MB database     │
│  Google AI    │  $0/mo   │  60 requests/min    │
├─────────────────────────────────────────────────┤
│  TOTAL        │  $0/mo   │  Perfect for demos! │
└─────────────────────────────────────────────────┘
```

---

## Troubleshooting Flow

```
Problem: Frontend can't connect to backend
        │
        ▼
    Check 1: Is REACT_APP_API_URL set correctly?
        │
        ├─ No → Add to Vercel env vars → Redeploy
        └─ Yes ▼
    Check 2: Is backend URL accessible?
        │
        ├─ No → Check Render logs → Fix errors
        └─ Yes ▼
    Check 3: CORS configured?
        │
        ├─ No → Update main.py → Redeploy
        └─ Yes ▼
    Check 4: Browser console errors?
        │
        └─ Check network tab → Verify request
```

---

## 🚀 Ready to Deploy?

**Start here**: Open `QUICK_DEPLOY.md` and follow the steps!

**Time estimate**: 10-15 minutes total
**Difficulty**: Easy (just copy/paste)
**Result**: Live app on the internet! 🌐
